const root = 'index';
const settings = 'settings';
const backroot = '/'

export const getRoot = () => {
    return root;
}

export const getSettings = () => {
    return settings;
}

export const getBackRoot = () => {
    return backroot;
}
